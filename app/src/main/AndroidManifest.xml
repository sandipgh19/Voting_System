<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sandip.voting_system">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/onlinevoting"
        android:label="Voting System"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Voting System"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".Admin" />
        <activity android:name=".Profile" />
        <activity android:name=".registration" />
        <activity android:name=".Login" />
        <activity android:name=".Question_page" />
        <activity android:name=".Quary_page" />
        <activity android:name=".question_answer" />
        <activity android:name=".Add_follower" />
        <activity android:name=".Followers"/>
        <activity android:name=".Upload"/>
    </application>

</manifest>